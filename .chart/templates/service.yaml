apiVersion: v1
kind: Service
metadata:
  name: {{ include "slogin.fullname" . }}
  labels:
    {{- include "slogin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slogin.selectorLabels" . | nindent 4 }}
